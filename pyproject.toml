[project]
name = "ekantipur-scraper"
version = "0.1.0"
description = "Web scraper for ekantipur.com"
requires-python = ">=3.13"
dependencies = [
    "playwright>=1.42.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]
